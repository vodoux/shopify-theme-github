{% comment %}
  This snippet is used to render the pickup availability component on the product page.
  It is used to display the pickup availability of the product in the store.
  Accepts:
    - product: {Object} The product object
    - classes: {String} CSS Classes on the wrapper element.
    - current_variant: {Object} The current variant object
    - block: {Object} The block object
    - shop: {Object} The shop object
    - routes: {Object} The routes object
  Usage:
    {% render 'snip-product-pickup-availability', classes: classes, product: product, current_variant: current_variant, block: block, shop: shop, routes: routes %}
{% endcomment %}
<pickup-availability
  class="{{ classes | default: 'product-single__store-availability-container' }}"
  {{ block.shopify_attributes }}
  data-block-id="{{ block.id | default: 'quickview' }}"
  data-store-availability-container
  data-product-title="{{ product.title | escape }}"
  data-has-only-default-variant="{{ product.has_only_default_variant }}"
  data-base-url="{{ shop.url }}{{ routes.root_url }}"
  data-variant-id="{{ current_variant.id }}"
></pickup-availability>